import 'package:flutter/material.dart';

class StudentsPages extends StatelessWidget {
  const StudentsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Students pages")),
    );
  }
}
